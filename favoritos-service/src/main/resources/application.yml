server:
  port: 8089

spring:
  application:
    name: favoritos-service
  datasource:
    url: jdbc:h2:mem:favoritosdb
    driverClassName: org.h2.Driver
    username: root
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
