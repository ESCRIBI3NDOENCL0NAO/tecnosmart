server:
  port: 8088

spring:
  application:
    name: envios-service
  datasource:
    url: jdbc:h2:mem:enviosdb
    driverClassName: org.h2.Driver
    username: root
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/