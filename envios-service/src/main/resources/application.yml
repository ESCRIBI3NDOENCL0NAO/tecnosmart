server:
  port: 8088

spring:
  application:
    name: envios-service
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_envios_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/