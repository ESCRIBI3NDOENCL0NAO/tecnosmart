server:
  port: 8090

spring:
  application:
    name: notificaciones-service
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_notificaciones_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
